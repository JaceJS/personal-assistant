# Calendar domain

Coming soon.

This domain is a placeholder. When implemented it will follow the same
structure as `app/domains/finance/`: `models.py`, `schemas.py`,
`repository.py`, `service.py`, `router.py`, and `extractor.py`.
